import { BackendService } from "./backend.service";

describe('BackendService tested using Jest', () => {

  let backendService: BackendService;

  beforeEach(() => {
    //
  });

  test('getGrape using Jest', () => {
    expect(2).toEqual(3);
  });

});
